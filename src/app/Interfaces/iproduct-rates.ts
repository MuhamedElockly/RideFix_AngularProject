export interface IproductRates {
  // value: number;
  // comment: string;
  // userId: string;
  // userName: string;
  // createdAt: string;

  value: number;
  comment: string;
  userId: string;
  userName: string;
  createdAt: string;

}
